name: Push to Repository A

on:
  push:
    branches:
      - master

jobs:
  trigger-repo-b:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          repository: Avadhraj/repository-dispatch@v3
          ref: master

      - name: Trigger Repository B
        uses: Avadhraj7/repository-dispatch@v3
        with:
          token: ${{ secrets.REPO_B_TOKEN }}
          repository: Avadhraj7/VepaarJenkin
          event-type: run-tests
